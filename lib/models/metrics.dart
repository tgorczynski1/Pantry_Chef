class Metrics {
  final String email;
  final String name;
  final String id;

  Metrics({this.email, this.name, this.id});
}

// Metrics are the profile settings stored for each User in the DB
//*these are used to track the login status of a user
//!THESE ARE NOT FOR UPDATING THE DATABASE
